export interface ProductVersion {
    compareNumber: number,
    number: number,
    productId: number,
    releaseDate: Date | string,
    versionCode: number,
    versionId: number,
    versionNumber: string
}